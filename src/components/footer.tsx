import React, { memo } from "react";

const Footer = memo(({}) => (
  <div className="bg-black text-gray-200 text-xs py-2 mt-4">
    <div className="max-w-main m-auto px-8">2019 © Kazushi Kawamura.</div>
  </div>
));

export default Footer;
